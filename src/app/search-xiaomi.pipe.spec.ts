import { SearchXiaomiPipe } from './search-xiaomi.pipe';

describe('SearchXiaomiPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchXiaomiPipe();
    expect(pipe).toBeTruthy();
  });
});
