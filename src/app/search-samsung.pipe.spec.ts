import { SearchSamsungPipe } from './search-samsung.pipe';

describe('SearchSamsungPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchSamsungPipe();
    expect(pipe).toBeTruthy();
  });
});
